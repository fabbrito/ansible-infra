---
# A board with role defaults: one key, no timezone key, generation 1.
#
# EVERY VALUE BELOW IS FAKE. Derived by the role, not declared in defaults:
# each fixture sets them.
seed_host_kind: board
seed_hostname: board-a
seed_user: operator
seed_authorized_keys:
  - ssh-ed25519 AAAAgolden-fixture-not-a-key operator@example
