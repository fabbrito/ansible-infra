# Rendered by Ansible — do not edit on host.
# Source: fabbrito.infra collection — roles/os/templates/99-infra-ntp.conf.j2
[Time]
# NTP= appends; the empty one drops servers set by earlier files.
NTP=
NTP=ntp.example.org 192.0.2.123
