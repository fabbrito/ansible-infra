---
# timesyncd has no reload. Never starts one the operator stopped.
- name: Restart timesyncd
  ansible.builtin.systemd_service:
    name: systemd-timesyncd
    state: restarted
  when: os_timesyncd_status.rc | default(1) == 0
