---
# journald always persists to /var/log/journal, so moving it onto the storage volume
# is a bind mount, not a config line. storage_path is a contract var (README),
# asserted absolute and mounted; journal_dir is relative to it.
journal_dir: journal
