---
# Present so role discovery sees this role — see roles/os/meta/main.yml.
# No deps, though service roles are tempted: play order delivers docker, and a
# meta dep would re-walk it on every service deploy.
dependencies: []
