---
# Reload, not restart: `fail2ban-client reload` (the unit's ExecReload) re-reads
# the jails in place and keeps the running ban table; a restart flushes it.
- name: Reload fail2ban
  ansible.builtin.service:
    name: fail2ban
    state: reloaded
  when: not ansible_check_mode
